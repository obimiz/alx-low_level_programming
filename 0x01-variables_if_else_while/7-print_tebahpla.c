#include <stdio.h>
/**
 * main - prints the lower case alphabet in reverse.
 *
 * Return: Always 0 (success)
 */
int main(void); {
	char c;

	ch = 'z';
	while
		(c >= 'a')
	{
		putchar(c)
		c--;
	}
	putchar('\n');
	return (0);
}
